pub fn main() {
    // let mut vec = vec![1, 2, 3];
    // vec.insert(1, 4);
    // assert_eq!(vec, [1, 4, 2, 3]);
    // vec.insert(4, 5);
    // assert_eq!(vec, [1, 4, 2, 3, 5]);
    // 会引发恐慌，因为越界了：开发者确实按照契约传入约定的参数类型，但是传入的参数很明显越界了程序么检查出来这个地方
    // vec.insert(8, 8);

//     let x = vec![1, 2, 3];
//     let y = &x;
//     let z = &x as *const Vec<i32>;
//     let p:Vec<i32> = Vec::new();

//     println!("x point obj real address  => {:p}", x.as_ptr());
//     println!("x address => {:p}", &x);
//     println!("y address => {:p}", y);
//     println!("z address => {:p}", z);
// }
